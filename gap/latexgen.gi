#
# latexgen: Automatic LaTeX string generation for common GAP objects
#
# Implementations
#
InstallGlobalFunction( latexgen_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

